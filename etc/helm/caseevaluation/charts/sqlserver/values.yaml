port: 1433
# Placeholder. Production deployments MUST override via --set saPassword=...
# or a values file kept out of version control.
saPassword: "CHANGE_ME_IN_PROD"
image:
  repository: "mcr.microsoft.com/azure-sql-edge"
  tag: "1.0.7"
  pullPolicy: IfNotPresent

resources:
  limits:
    memory: 4Gi
    cpu: 2000m
  requests:
    memory: 2Gi
    cpu: 500m
