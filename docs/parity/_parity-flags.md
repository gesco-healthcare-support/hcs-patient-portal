# Parity flags

Tracks behaviors where NEW deviates from OLD (or where OLD's behavior is
ambiguous and we have replicated it pending test verification). Per
project CLAUDE.md, every flag here pairs with a `// PARITY-FLAG:`
comment on the relevant C#/TypeScript line.

Status legend:
- `needs-test` — replicated OLD; manual test pending to confirm intent
- `parity-plus` — NEW deliberately exceeds OLD; OLD did not have this
- `bug-fix` — silently corrected an OLD bug; not preserved
- `resolved` — flag closed (manual test passed or replaced with verbatim port)

| Flag | Feature | OLD source citation | Description | Status |
|------|---------|---------------------|-------------|--------|
| PF-001 | Reminder jobs / JDF | OLD has no dedicated JDF reminder template | NEW's `PackageDocumentReminderEmailHandler` fires for both package-doc and JDF reminders. For JDF, the closest OLD-verbatim template code is `AppointmentDueDateUploadDocumentLeft` (TemplateCode 15, "due-date + pending docs left"). OLD's actual JDF flow is auto-cancel via `AppointmentCancelledDueDate` without a separate reminder. Mapping NEW's JDF reminder to `AppointmentDueDateUploadDocumentLeft` is best-effort semantic parity until N1 (Stage 7) revisits the JDF reminder job and confirms intent. | needs-test |
| PF-002 | CustomField rendering | OLD `PatientAppointment.DbEntities\Enums\CustomFieldType.cs` declares all 7 values; OLD Angular templates render only Alphanumeric / Numeric / Date | NEW carries all 7 enum values verbatim (Alphanumeric=12, Numeric=13, Picklist=14, Tickbox=15, Date=16, Radio=17, Time=18) for schema parity. Stage 2 task **B1** is parity-plus: the booking form will render all 7 input types (Picklist / Radio / Tickbox / Time + the original 3) so IT-Admin-defined custom fields work with every declared type. OLD's enum-vs-render mismatch was a latent bug; NEW closes it. | parity-plus |
| PARITY-FLAG-NEW-001 | Registration field set | OLD `users-add.component.html` collects only Email / FirstName / LastName / FirmName / Password at registration; demographics (DOB, address, phone, language) are captured later at booking time | NEW's AuthServer Razor `Register` page collects DOB at registration to satisfy the Patient profile's `dateOfBirth` required field on first login. Reverts the OLD "demographics-at-booking" model until external-user portal reaches multi-tenant scope. Bug-batch-2 issue #3 (FYI). Revisit when external-user portal grows beyond the demo tenant. | parity-plus |
| PARITY-FLAG-NEW-002 | Multi-select slot generation | OLD `doctors-availability-add.component.html:51-54` is a single `<select>`; UI sends a 1-row array with hardcoded `AppointmentTypeEnum.PQME` (`:76`) | NEW frontend fan-out: location and appointment-type are multi-select; the Generate Preview submits the cartesian product to the existing list-style backend (`DoctorAvailabilitiesAppService.GeneratePreviewAsync` already accepts `List<DoctorAvailabilityGenerateInputDto>`). Bug-batch-2 issue #2. Backend was already list-shape; the deviation is purely UI ergonomics. | parity-plus |
| PARITY-FLAG-NEW-003 | Admin nav pending-count badge | OLD `side-bar.component.html` has no badges; pending counts only appear on the dashboard tile cards (`dashboard.component.html:18`) | NEW renders a count badge on the Appointments sidebar entry whenever the current admin / staff supervisor / clinic staff user has unapproved (status=Pending) appointments awaiting decision. Backed by a new `IAppointmentsAppService.GetPendingCountAsync()` endpoint and a 60s Angular signal poll that patches the route name via `RoutesService.patch`. Bug-batch-2 issue #6. OLD's dashboard tile semantics inform but do not constrain the badge. | parity-plus |
| PARITY-FLAG-NEW-004 | English locks interpreter | OLD `appointment.domain.ts:650-661` only flips an `isInterpreter` validator on `interpreterVendorName` maxlength; OLD has zero coupling between language=English and the interpreter radio | NEW: when patient language is set to English, the "Do you need an interpreter?" radio is forced to No and disabled; switching language back to anything else re-enables the radio. Implemented as a `valueChanges` subscriber on `appointmentLanguageId` with a one-time `getAppointmentLanguageLookup({ filter: 'English' })` cache. Bug-batch-2 issue #15. | parity-plus |
| PARITY-FLAG-NEW-005 | Self-represented confirmation modal | OLD `appointment-add.component.html:662, 732` attaches `(change)="addValidationFor*Attorney(...)"` with no confirmation prompt | NEW (planned): toggling AA / DA off prompts a "Patient is self-represented?" confirmation via ABP `ConfirmationService` from `@abp/ng.theme.shared`. Cancel reverts the toggle. Bug-batch-2 issue #18. Implementation deferred to a future wave. | needs-test |
