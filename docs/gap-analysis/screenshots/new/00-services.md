# Track 9 NEW-side capture -- services + seed log

**Capture session start:** 2026-04-24 (Pacific local time).
**Capture worktree:** `W:\patient-portal\docs-track-9-new-capture-2026-04-24`
(branch `docs/track-9-new-capture-2026-04-24`).
**Backing services worktree:** `W:\patient-portal\main` (compose project
`main`, default ports).

## Running services (post-bring-up)

Brought up via `docker compose up -d --wait` from `W:\patient-portal\main`.
All five containers reported healthy after 2 min (no rebuild needed; named
volume `main_sqldata` persisted from a prior session, but the DB inside it
was empty of seed data).

| Service           | Container          | Image / source                              | Host port | Container port | Status    | Probe URL                                            | Result    |
|-------------------|--------------------|---------------------------------------------|-----------|----------------|-----------|------------------------------------------------------|-----------|
| sql-server        | main-sql-server-1  | mcr.microsoft.com/mssql/server:2022-latest  | 1434      | 1433           | healthy   | (compose healthcheck)                                | OK        |
| redis             | main-redis-1       | redis:7-alpine                              | 6379      | 6379           | healthy   | (compose healthcheck)                                | OK        |
| db-migrator       | main-db-migrator-1 | DbMigrator/Dockerfile                       | n/a       | n/a            | exited 0  | n/a                                                  | OK        |
| authserver        | main-authserver-1  | AuthServer/Dockerfile                       | 44368     | 8080           | healthy   | http://localhost:44368/.well-known/openid-configuration | HTTP 200 (0.47s) |
| api (HttpApi.Host)| main-api-1         | HttpApi.Host/Dockerfile                     | 44327     | 8080           | healthy   | http://localhost:44327/swagger/index.html            | HTTP 200 (0.08s) |
| angular           | main-angular-1     | angular/Dockerfile (nginx)                  | 4200      | 80             | healthy   | http://localhost:4200/                               | HTTP 200 (0.01s) |

Probe timestamp: 2026-04-24T15:36:17-0700 (post-bring-up, pre-seed).
Health endpoints also returned HTTP 200 on
`http://localhost:44327/health-status` and
`http://localhost:44368/health-status`.

Note: docker uses **HTTP** for both AuthServer and HttpApi (per
`docs/runbooks/DOCKER-DEV.md`'s "What This Setup Does NOT Do" section --
`AuthServer__RequireHttpsMetadata: false` for dev convenience). The
gap-analysis README (line 305) uses `https://localhost:44368` -- that
matches the dotnet-run path against LocalDB, not the docker-compose path.
This capture targets the docker-compose endpoints over HTTP.

## Seed run

The persisted `main_sqldata` volume held an empty `CaseEvaluation` DB at
bring-up time (the 2026-04-20 Master-Seed run targeted a prior backing,
not this volume). Re-seeded via `scripts/Master-Seed.ps1` so the capture
roster matches the briefing.

**Command** (run from `W:\patient-portal\main`):

```bash
TEST_PASSWORD='1q2w3E*' powershell.exe -File scripts/Master-Seed.ps1 \
  -ApiBaseUrl http://localhost:44327 \
  -AuthServerUrl http://localhost:44368 \
  -SkipPrerequisites
```

`scripts/seed-state.json` was deleted before the run so phase A1 started
from an empty hashtable (avoids stale GUIDs from the 2026-04-20 backup;
the .bak is preserved at `scripts/seed-state.json.bak`).

**Run total:** 74s. Phases A1 through A6 all completed.

**Verification block (script's own assertions):**

| Reference data       | Count | Expected | Status |
|----------------------|-------|----------|--------|
| States               | 16    | >= 10    | PASS   |
| Appointment Types    | 6     | >= 6     | PASS   |
| Appointment Statuses | 13    | >= 13    | PASS   |
| Appointment Languages| 16    | >= 12    | PASS   |
| Locations            | 16    | >= 8     | PASS   |
| WCAB Offices         | 7     | >= 7     | PASS   |
| T1 Appointments      | 13    | >= 13    | PASS   |
| T2 Appointments      | 10    | >= 10    | PASS   |
| T3 Appointments      | 5     | >= 5     | PASS   |
| T4 Appointments      | 0     | >= 0     | PASS   |
| T5 Appointments      | 0     | >= 0     | PASS   |

(States count = 16 instead of the original 10 because phase A2's earlier
attempt-and-retry left 6 active locations + 1 manual debug location in the
DB before the clean re-run -- this inflates Locations to 16 vs the
typical 8, but does not affect role-coverage capture.)

## Seeded user roster (post-seed)

Fresh emails generated by the re-run; replaces the briefing's pre-cached
roster. All passwords are `1q2w3E*` (ABP default; matches
`$env:TEST_PASSWORD` for Master-Seed).

| Tenant | Doctor / TenantAdmin              | ApplicantAttorney         | DefenseAttorney        | ClaimExaminer         | Patient (P1 / P2 / NullPatient)               |
|--------|-----------------------------------|---------------------------|------------------------|-----------------------|-----------------------------------------------|
| host   | admin@abp.io (TenantId null, role `admin`) | --                        | --                     | --                    | --                                            |
| T1     | anahit.thomas@hcs.test            | aram.miller@hcs.test      | laura.reyes@hcs.test   | chen.nguyen@hcs.test  | christopher.anderson / tigran.martinez        |
| T2     | maria.rivera@hcs.test             | pedro.tran@hcs.test       | rachel.moore@hcs.test  | hayk.lopez@hcs.test   | sona.lee / lan.lewis                          |
| T3     | chen.gasparyan@hcs.test           | alejandro.lee@hcs.test    | sakura.liu@hcs.test    | ana.suzuki@hcs.test   | rafael.torres / sona.chavez                   |
| T4     | isabella.pham@hcs.test            | --                        | --                     | --                    | rachel.torres@hcs.test (T4_NullPatient)       |
| T5     | levon.khachatryan@hcs.test        | --                        | --                     | --                    | --                                            |

Tenant GUIDs (for `__tenant` header on `/connect/token`):

- T1: `5e3831bc-6dc6-c4c1-4185-3a20d3ef91f8`
- T2: `04e3a597-f365-62da-dd8e-3a20d3efca1b`
- T3: `e894abc3-3a66-8d10-f7c1-3a20d3efdd0c`
- T4: `8453c3a6-f6a2-62ca-fb04-3a20d3efe5ea`
- T5: `e6a6b759-b260-a271-4720-3a20d3efeed4`

## Login mechanics confirmed

- Host admin (`admin@abp.io` / `1q2w3E*`, no `__tenant` header):
  `POST /connect/token` -> 200, access_token length 1394.
- T1 Doctor (`anahit.thomas@hcs.test` / `1q2w3E*`,
  `__tenant: 5e3831bc-...`): `POST /connect/token` -> 200, access_token
  length 1506 (longer due to tenant + role claims).

OAuth client: `CaseEvaluation_App`. Scope:
`offline_access CaseEvaluation openid profile email roles`. Grant type:
`password`. The Angular login page will be exercised in sub-task C; the
direct token-grant probes here just verify credential validity before
the chrome-devtools-mcp capture starts.

## Caveats observed during bring-up

- **Phase A2 first attempt errored on the inactive-location POST.** Manual
  retry of the same body succeeded -- transient SQL hiccup, likely tied
  to the SQL container still warming up despite `--wait` reporting healthy.
  Resolved by deleting `scripts/seed-state.json` and re-running with
  idempotency (existing rows skipped, missing rows added). Final state
  has 16 Locations (6 from the first attempt + 1 manual debug + 9 from
  the second clean run).
- **Healthcheck UI background noise in api logs.** `HealthCheckReportCollector`
  logs `IPv4 0.0.0.0 ... cannot be used as target address` repeatedly
  every 10s. This is a known docker-mode issue with the HealthChecks UI
  hosted service trying to resolve a self-target; does not affect any
  user-facing endpoint. Captures will note this in the synthesis findings
  but the cosmetic warnings do not block the capture.

## What's next

- Sub-task B: write the credential matrix in the appendix (now grounded
  on this seeded roster) and verify each login via `/connect/token`
  before invoking chrome-devtools-mcp for the UI capture.
- Sub-task C: capture begins with the host admin (admin@abp.io), then
  T1 -> T2 -> T3 -> T4 -> T5 in the order documented in the plan.
